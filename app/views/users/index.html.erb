
Users
<table>
  <thead>
    <td>name</td>
  </thead>
  <% @users.each do |user| %>
  <tbody>
    <td><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no-user-image.png",size: "40x40" %></td>
    <td><%= user.id %></td>
    <td><%= user.name %></td>
    <td><%= link_to "Show", user_path(user) %></td>
    <td><%= link_to "Edit", edit_user_path(user) %></td>
    <% end %>
  </tbody>
</table>


<%= link_to "Back", articles_path %>